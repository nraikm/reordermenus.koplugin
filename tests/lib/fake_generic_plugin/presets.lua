-- Synthetic third-party plugin module: generic name "presets".
return {
    _fake_generic_plugin = true,
    name = "presets",
    description = "hostile generic module used by namespace-isolation tests",
}
